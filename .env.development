# Dev — used for `next dev`, dev Android builds (non-main branches), and build:android:dev.
# Telemetry stays off for dev flavor builds.

NEXT_PUBLIC_TEST_MODE=true
NEXT_PUBLIC_ANALYTICS_ENABLED=false
